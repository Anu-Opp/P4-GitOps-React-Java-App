apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ceeyit-backend.fullname" . }}
  labels:
    app: {{ include "ceeyit-backend.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ceeyit-backend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ceeyit-backend.name" . }}
    spec:
      containers:
      - name: backend
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8080
        resources:
          {{- toYaml .Values.resources | nindent 12 }}