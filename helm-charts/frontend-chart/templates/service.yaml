apiVersion: v1
kind: Service
metadata:
  name: {{ include "ceeyit-frontend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
    protocol: TCP
  selector:
    app: {{ include "ceeyit-frontend.name" . }}